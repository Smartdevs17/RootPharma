import { expect } from "chai";
import pkg from "hardhat";
const { ethers } = pkg;

describe("AuditLog", function () {
  it("Should deploy audit log mock", async function () {
    // Libraries are internal, typically need a mock to test events
    // Creating test stub
    expect(true).to.be.true;
  });
});
