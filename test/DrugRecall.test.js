import { expect } from "chai";
import { ethers } from "hardhat";

describe("DrugRecall", function () {
  it("Should initiate recall", async function () {
      // Test stub
    expect(true).to.be.true;
  });
});
