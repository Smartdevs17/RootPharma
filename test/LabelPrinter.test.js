import { expect } from "chai";
import { ethers } from "hardhat";

describe("LabelPrinter", function () {
  it("Should format ZPL", async function () {
      // Test stub
    expect(true).to.be.true;
  });
});
